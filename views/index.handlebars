<div class="row">
    <div class="col-md-6">
        <ul class="burger-list">
            {{#each burgers}}
            {{#unless this.devoured}}
            <li class="burger">{{this.id}}. {{this.burger_name}}
                <button class="devour-button float-right btn btn-primary" id="devour-button"
                    data-id="{{id}}" data-newdevour="{{devour}}">Devour</button>
            </li>
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="col-md-6">
        <ul class="burger-list">
            {{#each burgers}}
            {{#if this.devoured}}
            <li class="burger devoured">
                {{this.id}} {{this.burger_name}}
            </li>
            {{/if}}
            {{/each}}
        </ul>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <form action="/api/addburger" method="POST">
                    <input type="text" class="form-control" name="burger_name" id="enter-text">
                    <button type="submit" class="btn btn-primary" id="burger-submit">Submit</button>
                </form>
            </div>
        </div>
    </div>
</div>